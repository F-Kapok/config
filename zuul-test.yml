env: test
server:
  port: 8083
zuul:
  #忽略头部敏感共享如cookie
  sensitive-headers: 
  routes:
    myZuul:
      service-id: product
      path: /kapok-p/**
  ignored-patterns:
    - /**/product/listForOrder.do
management:
  endpoints:
    web:
      exposure:
        include: "*"
